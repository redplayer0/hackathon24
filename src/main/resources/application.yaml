spring:
  datasource:
    url: jdbc:sqlite:project.db
    driver-class-name: org.sqlite.JDBC
  jpa.show-sql: true
  jpa.properties.hibernate.dialect: org.hibernate.community.dialect.SQLiteDialect

management:
  endpoints:
    web:
      exposure:
        include: "*"
